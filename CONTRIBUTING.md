# 🤝 如何维护与更新本知识库

本知识库按**小步提交、持续推送**的节奏演进。无论独自维护还是开放协作，先读这份指南。

---

## 📁 项目结构（篇 → 章 → 节）

```
model_learing/
├── README.md                  # 总目录：编排逻辑、知识树、章节导航
├── ROADMAP.md                 # 知识地图与阅读路线
├── CONTRIBUTING.md            # 本文件：维护规范 + 扩展机制
├── CHANGELOG.md               # 更新日志
├── LICENSE                    # MIT 协议
├── templates/
│   └── 模型讲解模板.md         # 四级模板：节 / 章导览 / 篇导览 / 生产实战页
├── playground/                # 交互实验场：单文件 HTML，本地双击即可玩
│   ├── index.html
│   ├── knowledge-tree.html    # 交互式知识树（全书总览入口）
│   ├── gradient-descent.html  # 梯度下降炼丹炉
│   └── attention.html         # 注意力热力图
└── docs/
    ├── 1-起步准备/            # 第 0–2 章
    │   ├── README.md
    │   ├── 00-学前必读/
    │   ├── 01-环境与工具/     # 含 99-生产实战.md
    │   └── 02-数学基础/       # 含 99-生产实战.md
    ├── 2-经典机器学习/        # 第 3 章（含 99-生产实战.md）
    ├── 3-深度学习/            # 第 4 章（含 99-生产实战.md）
    ├── 4-专业方向/            # 第 5–11 章：视觉/时序/NLP与LLM/推荐/图/强化/因果
    │   ├── README.md
    │   ├── 05-计算机视觉/ 06-时间序列/ 07-自然语言处理与LLM/
    │   └── 08-推荐系统/ 09-图神经网络/ 10-强化学习/ 11-因果推断/
    └── 5-前沿与融合/          # 第 12 章（含 99-生产实战.md）
        └── 12-生成模型与多模态/
```

- **篇**：数字前缀文件夹 + 篇导览 `README.md`
- **章**：两位数字前缀文件夹（章号全书连续 00–12）+ 章导览 `README.md`
- **节**：章内 `NN-页面名.md`；**每章收官页固定命名 `99-生产实战.md`**（第 0 章除外）

---

## 🧩 扩展机制（本库为可生长而设计）

新技术层出不穷（世界模型、新架构、新模态……），按下面三级落位，**只往后加，不推倒重来**：

| 情形 | 做法 | 落位 |
|------|------|------|
| 出现重要的新模型/新概念，还没看透 | 在第 12 章「前沿技术地图」页**登记一个条目**（一段直觉 + 为什么重要 + 观察点） | 前沿地图 = 新技术蓄水池 |
| 该技术积累了足够内容（≥1 页） | **独立成页**：挂到最相关的章，文件名用该章下一个可用编号（不动已有编号，`99-生产实战.md` 永远是收官） | 最相关章 |
| 长成一个完整方向 | **独立成章**：章号 = 当前最大章号 +1（当前到 12），归入第四篇（专业方向）或第五篇（前沿与融合），并同步更新：两个篇 README、根 README 目录与知识树、ROADMAP、CHANGELOG | 第四/五篇末尾 |

示例：世界模型路线（JEPA / V-JEPA 类）目前在第 12 章前沿地图有条目；若日后内容成熟，可独立成页挂入第五篇，再成熟可成章。

### 新增一章的检查单

- [ ] 新章文件夹 `NN-章名/` + `README.md`（模板二）+ 正文页（模板一）+ `99-生产实战.md`（模板四）
- [ ] 前后章的翻页导航（上一章 / 下一页）改为指向新章
- [ ] 更新：所在篇 README、根 README（目录 + 知识树 mermaid）、ROADMAP、CHANGELOG

---

## ✍️ 内容十条黄金法则

1. **新页面必须从模板开始**：复制 `templates/模型讲解模板.md` 对应模板，保留全部小节。
2. **文件命名**：`NN-中文名.md`，不用空格与 ASCII 特殊符号；篇用一位数字前缀，章号全书连续。
3. **通俗优先**：先"解决什么问题"和生活化类比，再进公式。默认读者只会一点 Python。
4. **术语标英文**：首次出现写中文（English），如"梯度下降（Gradient Descent）"。
5. **每页至少一张 mermaid 图**，标签**必须双引号**（`A["卷积层"]`）；图型不拘泥于流程图，见下方「图型菜单」。
6. **公式必须配人话**：块级 `$$...$$`（独占空行），行内 `$...$`，公式后跟"👉 人话：……"。
7. **代码要能跑**：最小可运行示例，注释中文，说明预期输出。
8. **定位是教材与知识分享，不是培训课程**：不写"X 天 / X 周 / X 个月学会"的时间承诺；不喊口号。
9. **链接用相对路径**（速查表见下）；不存在的页面只写文字不放假链接。
10. **写完三处同步**：本章 `README.md`（含 99 页）、根 `README.md` 目录与进度、`CHANGELOG.md`。

### 易懂三件套（每页必备，硬性标准）

1. **📋 例子**：模板一"举个例子"节不可省——带**具体数字或小场景**走一遍（如 5 个点的人工梯度下降、3 词句子的注意力权重表）；长算例放 `<details>` 折叠。
2. **🖼️ 图**：每页 ≥1 张 mermaid 图；鼓励在核心概念处多放"小而准"的图——一张图讲一个概念，优于一张大图讲十个概念。
3. **🖱️ 交互**：每页 ≥2 处 `<details>` 折叠（自测 + 一个算例/推导/进阶）；页面相关时必须加 🎮 本库 playground 或 🕹️ 外部交互工具链接（见下表）。

#### 🕹️ 常用外部交互资源（稳定的经典工具，按主题取用）

| 资源 | 链接 | 适用页面 |
|------|------|----------|
| TensorFlow Playground | https://playground.tensorflow.org | 神经网络、训练流程、过拟合 |
| CNN Explainer | https://poloclub.github.io/cnn-explainer/ | CNN、特征图 |
| Transformer Explainer | https://poloclub.github.io/transformer-explainer/ | Transformer、注意力、LLM |
| Embedding Projector | https://projector.tensorflow.org | 词向量、降维 |
| Seeing Theory | https://seeing-theory.brown.edu | 概率统计 |

### 每章收官：99-生产实战.md

每章正文之后必有一页「生产实战」（模板四），回答**真实生产怎么做**：项目从 0 到 1（需求→数据→设备算力→算法选型→训练→评估→部署→监控迭代）、场景选型表、真实工具链、踩坑实录、炼丹师大实话。**真实性行规**：工具用真名；数字给行业常见量级并注明"经验参考"；只写通用行业实践，不编造公司内部细节。

### 🔥 炼丹小灶

主题页中鼓励加「炼丹小灶」标注框（模板一可选节）：工业界起点配方（超参、训练配置）与玄学（换种子、重启大法），注明"经验参考，不是圣旨"。

---

## 🎨 mermaid 图型菜单（不要只用流程图）

GitHub 原生渲染以下图型，按内容选最合适的（示例可直接复制改用）：

| 图型 | 适用场景 | 最小示例 |
|------|----------|----------|
| `graph TD/LR` | 流程、架构、依赖 | `graph TD` + `A["x"] --> B["y"]` |
| `timeline` | 演化史、发展时间线 | `timeline` + `title 标题` + `2017 : 事件` |
| `quadrantChart` | 选型象限（两维权衡） | `quadrantChart` + `x-axis 低 --> 高` + `"方案A": [0.3, 0.7]` |
| `pie` | 预算 / 时间占比 | `pie showData` + `title 标题` + `"GPU" : 60` |
| `gitGraph` | 工程流程、分支协作 | `gitGraph` + `commit id:"init"` + `branch dev` |
| `sequenceDiagram` | 系统交互、请求链路 | `sequenceDiagram` + `A->>B: 请求` |

注意：flowchart 节点标签双引号；`timeline`/`quadrantChart` 对缩进敏感，写完先贴 [mermaid.live](https://mermaid.live) 验证再提交。

---

## 🎮 playground 交互实验场规范

- 每个实验 = **单个自包含 HTML 文件**（内联 CSS/JS，零依赖、零构建），克隆后**本地双击即可打开**；启用 GitHub Pages 可在线访问。
- 新增实验：`playground/NN-实验名.html`，并在 `playground/index.html` 登记入口。
- 页面要求：中文 UI、可交互（滑块/输入）、视觉直观、代码注释中文。
- 在正文页相应位置链接实验：`> 🎮 动手玩：[梯度下降炼丹炉](../../../playground/gradient-descent.html)`

---

## 相对路径速查（页面与章 README 位于 `docs/篇/章/` 层）

| 目标 | 相对路径 |
|------|----------|
| 根目录 README | `../../../README.md` |
| 本篇导览 | `../README.md` |
| 本章 README | `./README.md` |
| 同章其他页 | `./NN-页面名.md` |
| 同篇其他章 | `../章文件夹/NN-页面名.md` |
| 其他篇 | `../../篇文件夹/章文件夹/NN-页面名.md` |
| playground 实验 | `../../../playground/实验名.html` |

篇 README（位于 `docs/篇/` 层）：根目录 `../../README.md`，本篇章 `./章文件夹/README.md`，他篇 `../篇文件夹/README.md`。

### 翻页导航规范

- 页面顶部：`[⬆️ 返回总目录](../../../README.md) · [⬆️ 返回本篇](../README.md) · [⬅️ 返回本章目录](./README.md)`
- 页面底部：`[⬅️ 上一页：…] · [返回本章目录](./README.md) · [下一页：… ➡️]`；本章首页"上一页"= 本章 README；正文末页"下一页"= `./99-生产实战.md`；99 页"下一页"= 下一章 README（章序 00→…→12 全书连续；跨篇时路径为 `../../篇文件夹/章文件夹/README.md`）
- 章 README 顶部：返回总目录 + 返回本篇；底部：上一章 / 下一章（第 12 章为全书完）

---

## 🔄 更新工作流（小步提交，按节奏推送）

```bash
# 每次完善一个主题，就完成一轮"改 → 记 → 推"
git add .
git commit -m "docs(08-推荐): 新增双塔模型负采样细节"
git push
```

- commit 格式：`docs(章号-章节名): 做了什么`；一次 commit 只做一件事
- 推送前跑一遍自查清单；开放协作时：fork → 分支 → PR，流程不变

### 提交自查清单

- [ ] 新页面从模板复制、小节齐全？
- [ ] ≥1 张 mermaid 图，标签双引号，[mermaid.live](https://mermaid.live) 验证可渲染？
- [ ] 每个公式后有人话？代码可运行？
- [ ] 相对链接全部有效？（跑一遍 `python tools/check_links.py`，可加 `--fix` 自动修复层级笔误）
- [ ] 本章 README（含 99 页）、根 README、CHANGELOG 三处已同步？
- [ ] 若新增章：是否完成「新增一章的检查单」？

---

## 📋 CHANGELOG 格式

```markdown
## [版本] - YYYY-MM-DD

### 新增
- 新增了 xxx（#页面链接）

### 修订
- 修正了 xxx 的说法 / 图 / 代码

### 规划中
- 下一步准备写 xxx
```

---

感谢你让这棵树长出新的枝叶 🌱
